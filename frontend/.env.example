VITE_API_BASE_URL=http://localhost:8000

# CDN URL for chunk images (CloudFront) - optional
# If set, chunks load directly from CDN instead of through the API
VITE_CDN_BASE_URL=https://d1234abcd.cloudfront.net
