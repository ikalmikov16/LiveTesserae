# Test dependencies. Pinned for the same reason as requirements.txt: an
# unrelated pytest release should not be able to turn a green suite red.
#
#   pip install -r requirements.txt -r requirements-test.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-mock==3.15.1
httpx==0.28.1
